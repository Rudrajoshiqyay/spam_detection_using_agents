# FraudGuard AI — Frontend Environment Variables
# Copy to .env.local for local development

# Backend API URL (leave empty to use Vite proxy /api → localhost:8000)
VITE_API_URL=

# Set to 'true' to force mock data even if API is reachable
VITE_MOCK_MODE=false
