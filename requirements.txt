# ============================================================
# FraudGuard AI — Python 3.11 required (see runtime.txt)
# All packages have pre-built wheels for Python 3.11.
#
# LangChain 1.x ecosystem — all three packages must be from
# the same major-version family to share a compatible core:
#
#   langchain-core  1.4.x  ← shared foundation
#   langgraph       1.2.x  ← requires langchain-core >=1.4.7
#   langchain-groq  1.1.x  ← requires langchain-core >=1.x
#
# Removed:
#   langchain-openai 1.3.3  — version exists but project uses Groq,
#                             not OpenAI; replaced by langchain-groq
#   openai >=1.51.2         — only used for error types; the groq
#                             SDK (installed by langchain-groq) exposes
#                             the same RateLimitError / APIStatusError
#                             hierarchy
# ============================================================

# API framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.2
pydantic-settings>=2.5.2

# LangChain 1.x ecosystem (native Groq integration, no OpenAI dependency)
langgraph==1.2.5
langchain-core==1.4.8
langchain-groq==1.1.3
greenlet>=3.0.0

# Retry / resilience
tenacity>=8.3.0

# Redis
redis==5.1.1
hiredis==3.0.0
fakeredis>=2.26.0

# Rate limiting
slowapi>=0.1.9

# Metrics
prometheus-fastapi-instrumentator>=7.0.0

# Graph analysis
networkx==3.3

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
aiosqlite==0.20.0

# Utilities
python-dotenv==1.0.1
httpx==0.27.2
structlog==24.4.0
